/*
  JavaScript function is a block of code designed to perform a particular task.
*/

// Declarative

function miFunction() {
  return 3;
}
miFunction();

// Expresive
var miFunction = function(a,b) {
  return a + b;
}
miFunction();

// Functions Examples / Students = Arguments
function onboardingStudents(student) {
  console.log(student);
}